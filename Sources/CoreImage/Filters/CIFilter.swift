import Foundation

/// An image processor that produces an image by manipulating one or more input images or by generating new image data.
@MainActor public class CIFilter {
    public init() {
    }
}